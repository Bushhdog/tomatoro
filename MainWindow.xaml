<Window x:Class="Tomatoro.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tomatoro"
        xmlns:vm="clr-namespace:Tomatoro.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow - Tomatoro" Height="450" Width="800">
        <Window.DataContext>

            <vm:MainViewModel />
            
        </Window.DataContext>
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">

        <TextBlock Text="{Binding TempoFormatado}" />
        
        <Button Command="{Binding IniciarCommand}" Content="Iniciar" />
        <Button Command="{Binding PausarCommand}" Content="Pausar" />
        <Button Command="{Binding ResetarCommand}" Content="Resetar" />

    </StackPanel>
</Window>
